Critical (These are breaking you right now)                                                                                                                                                                                           
  1. No pagination — fetches ALL businesses every request  api/businesses.php has no LIMIT clause. Every page load pulls every single business row (descriptions included)     into memory. This is almost certainly your primary culprit.                                                                                                                                                                             2. Missing database indexes                                                                                         The businesses table has no indexes on is_approved, category, country, or canton — so every filtered query does a   full table scan.                                                                                                                                                                                                                        3. LIKE search without a FULLTEXT index                                                                             api/businesses.php uses LIKE '%query%' which can't use any index. Full table scan on every search.                                                                                                                                      ---                                                                                                               
  High Impact

  4. Client-side filtering instead of server-side
  BusinessesContent.tsx loads all businesses into React state, then filters in the browser. With thousands of rows  
  and search input, this freezes the UI.

  5. No Redis/APCu query caching
  Business data barely changes but is re-queried from MySQL on every single request.

  6. 900+ coordinates hardcoded in MapView.tsx
  MapView.tsx is ~33,000 tokens of hardcoded coordinate data. Adds ~50-100KB to the JS bundle loaded on every page. 

  7. No gzip compression
  Responses are sent uncompressed. JSON + HTML payloads are 5-10x larger than they need to be.

  8. Unnecessary JOIN — owner data on every request
  businesses.php always JOINs the users table to get owner name/email, even for public visitors who never see that  
  data.

  ---
  Medium Impact

  9. Cache-Control too short (5 min)
  Public business list could be cached for 1 hour minimum, with stale-while-revalidate.

  10. JWT verified on every request with no caching
  Each protected API call does a full HMAC-SHA256 computation. Under load this taxes CPU.

  11. Map blocks page render
  showMap defaults to true, so Leaflet/MapView must load before the business list is usable.

  ---
  Quick Wins (fix in minutes)

  -- Run these on your MySQL database RIGHT NOW for an immediate speedup:
  CREATE INDEX idx_businesses_approved  ON businesses(is_approved);
  CREATE INDEX idx_businesses_category  ON businesses(category);
  CREATE INDEX idx_businesses_country   ON businesses(country, canton);
  ALTER TABLE businesses ADD FULLTEXT INDEX idx_search (name, name_fa, description);

  // Add to businesses.php GET handler — immediate relief:
  $limit  = intval($_GET['limit'] ?? 50);
  $offset = intval($_GET['page']  ?? 0) * $limit;
  $sql   .= " LIMIT $limit OFFSET $offset";

  The database indexes + pagination fix alone will likely bring your load time from 15+ seconds back under 1 second.
   Want me to implement any of these?


That confirms it. Your server is in the USA, your users are in Europe/Switzerland/Iran — every API call travels  across the Atlantic and back. With no caching and no CDN, each page load does that round trip multiple times.     
                                                                                                                      Fastest fix: enable Cloudflare today (free, 10 minutes)
                                                                                                                    
  1. Go to cloudflare.com → Add site → enter birunimap.com
  2. Cloudflare scans your DNS records → click Continue
  3. Change your domain's nameservers to the two Cloudflare ones they give you (do this at wherever you bought the  
  domain — GoDaddy, Namecheap, etc.)
  4. Wait 5–30 minutes for DNS to propagate
  5. In Cloudflare dashboard → Cache Rules → cache */api/businesses.php for 5 minutes

  After this, Swiss/European users will get responses from Frankfurt or Amsterdam instead of crossing to the USA.   

  ---
  Medium-term fix: migrate the PHP server to Europe

  Hetzner (Germany) has servers for €4–6/month, excellent connectivity to both Europe and Iran, much better than any
   US host for your audience. Once Cloudflare is up, migration becomes low-risk because you can switch the origin   
  behind Cloudflare without users noticing any downtime.

  ---
  Do you want me to help you write the Cloudflare cache rules, or check if your domain registrar makes the
  nameserver change straightforward?

✻ Brewed for 8s      

